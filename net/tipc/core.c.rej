--- net/tipc/core.c
+++ net/tipc/core.c
@@ -69,9 +69,7 @@ static int __net_init tipc_init_net(struct net *net)
 	if (err)
 		goto out_nametbl;
 
-	err = tipc_topsrv_start(net);
-	if (err)
-		goto out_subscr;
+	INIT_LIST_HEAD(&tn->dist_queue);
 
 	err = tipc_bcast_init(net);
 	if (err)
