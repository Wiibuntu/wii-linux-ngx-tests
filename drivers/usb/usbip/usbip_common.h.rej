--- drivers/usb/usbip/usbip_common.h
+++ drivers/usb/usbip/usbip_common.h
@@ -268,6 +275,7 @@ struct usbip_device {
 	/* lock for status */
 	spinlock_t lock;
 
+	int sockfd;
 	struct socket *tcp_socket;
 
 	struct task_struct *tcp_rx;
