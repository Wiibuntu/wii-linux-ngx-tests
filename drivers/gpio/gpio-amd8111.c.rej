--- drivers/gpio/gpio-amd8111.c
+++ drivers/gpio/gpio-amd8111.c
@@ -245,6 +248,7 @@ static void __exit amd_gpio_exit(void)
 	gpiochip_remove(&gp.chip);
 	ioport_unmap(gp.pm);
 	release_region(gp.pmbase + PMBASE_OFFSET, PMBASE_SIZE);
+	pci_dev_put(gp.pdev);
 }
 
 module_init(amd_gpio_init);
